{
  "name": "fc-sdk-wechat-miniprogram",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "tsc -b packages/*",
    "clean": "rm -rf packages/*/dist packages/*/tsconfig.tsbuildinfo",
    "pack:all": "yarn build && cd packages/core && yarn pack --filename flashcatcloud-miniprogram-core.tgz && cd ../miniprogram-core && yarn pack --filename flashcatcloud-miniprogram-platform.tgz && cd ../miniprogram-rum && yarn pack --filename flashcatcloud-miniprogram-rum.tgz"
  },
  "devDependencies": {
    "typescript": "^5.8.3"
  }
}
